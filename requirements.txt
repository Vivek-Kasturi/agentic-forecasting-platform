# Core data stack
pandas>=2.0.0
numpy>=1.24.0

# Data ingestion (Kaggle dataset loader)
kagglehub>=0.2.0

# Environment configuration
python-dotenv>=1.0.0

# Logging (optional upgrade, works with current logging too)
loguru>=0.7.0

# Future-ready ML stack (safe to include now)
scikit-learn>=1.3.0

# Time series support (useful for forecasting later)
statsmodels>=0.14.0

# Optional but commonly used in forecasting pipelines
matplotlib>=3.7.0
seaborn>=0.13.0